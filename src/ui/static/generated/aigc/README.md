Runtime AIGC image outputs are written here by `POST /api/aigc/preview` when
`mode=live_image` succeeds.

Generated image files are ignored by git. Keep this README so the static
directory exists in a fresh checkout.
