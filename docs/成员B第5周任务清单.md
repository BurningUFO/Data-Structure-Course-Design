# 成员 B 第 5 周任务清单

## 1. 文档目的

本文档用于明确成员 B 在第 5 周需要完成的工作内容，作为本周协作、提交材料和设计方案讲解前的执行清单。

时间背景：

- 当前日期：2026-04-04（周六）
- 第 5 周设计方案讲解时间：2026-04-05（周日）

因此，本清单重点面向“本周末前必须完成”的内容。

---

## 2. 本周总目标

成员 B 本周的目标不是实现全部代码，而是先把自己负责模块的“设计方案材料”准备完整，确保：

1. 可以支撑 2026-04-05 的设计方案讲解
2. 可以被组员直接拿去做 PPT
3. 能清晰说明成员 B 后续具体负责什么
4. 为下周开始写代码建立统一方案

---

## 3. 本周必须完成的任务

下面按优先级整理。

### P0：今天必须优先完成

#### 任务 1：完成成员 B 的架构与逻辑方案文档

目标：

- 形成一份可直接提交和汇报的 Markdown 文档

最低应包含：

- 成员 B 负责的功能范围
- 查询、排序、推荐模块的职责边界
- 业务层、算法层、数据层之间的关系
- 与成员 A、成员 C 的接口依赖

建议产物：

- `docs/architecture/成员B架构与逻辑方案.md`

完成标准：

- 组员拿到这份文档后，不需要你口头补很多背景也能看懂

#### 任务 2：完成系统分层架构图

目标：

- 用 Mermaid 画出系统结构

最低应包含：

- 表现层
- 业务层
- 算法层
- 数据层
- 成员 A、成员 B、成员 C 模块关系

建议产物：

- 可以直接写进 `docs/architecture/成员B架构与逻辑方案.md`
- 或分别单独放在 `docs/architecture/系统总览分层架构图.md` 和 `docs/architecture/成员B细化架构图.md`

完成标准：

- 图能体现成员 B 在系统中的位置，而不是只写泛泛的“旅游系统”

#### 任务 3：完成“业务功能 - 数据结构 - 算法”映射表

目标：

- 满足第 5 周方案讲解中对“算法设计合理性”的说明要求

至少覆盖以下内容：

- 旅游推荐 -> Top-K / 排序 -> 最小堆、快速选择
- 景点/学校查询 -> 查找 + 排序 -> 精确查找、条件过滤、排序
- 场所查询 -> 查找 + 距离排序 -> 查找、过滤、调用最短路径距离
- 日记查询 -> 精确查找 / 目的地查找 -> 哈希或索引结构
- 模糊查询 -> 模糊匹配 -> 子串匹配、关键词匹配、可扩展编辑距离

建议产物：

- 放在 `docs/architecture/成员B架构与逻辑方案.md` 中单独成节

完成标准：

- 每一项都要写“为什么选这个结构/算法”，不能只列名词

#### 任务 4：补齐 AI 辅助开发证明材料

目标：

- 满足时间表中要求的“Cursor/Trae 使用截图”

至少准备：

- 2 到 3 张截图
- 能体现你在用 AI 进行架构设计或算法分析

建议存放位置：

- `docs/architecture/assets/`

完成标准：

- 截图内容和你的文档主题一致，不是随便截一张聊天窗口

---

### P1：本周内最好完成

#### 任务 5：整理成员 B 的接口依赖说明

目标：

- 让后续成员 A、成员 C 知道应该给你提供什么

至少明确：

- 成员 A 的距离接口输入输出
- 成员 C 的景点、美食、日记数据字段
- 全文检索结果如何返回给成员 B

建议产物：

- 写进 `docs/architecture/成员B架构与逻辑方案.md`
- 或补到 `docs/成员B开发计划.md`

完成标准：

- 接口依赖写清楚“我要什么字段”和“我要什么返回格式”

#### 任务 6：整理本周 PPT 可直接使用的素材

目标：

- 减少组员搬运和重写成本

建议你主动准备：

- 一页“成员 B 负责内容”
- 一页“查询与推荐模块分层架构”
- 一页“业务功能 - 数据结构 - 算法映射表”

完成标准：

- 每页都能直接复制到 PPT，不需要组员再帮你重写逻辑

#### 任务 7：确认本周讲解时你负责部分的讲述要点

目标：

- 防止明天讲解时 PPT 有内容但讲不清楚

建议准备 3 个口头重点：

1. 为什么成员 B 负责的是系统的业务交互中心
2. 为什么 Top-K 不能简单全量排序
3. 为什么场所和美食排序必须依赖真实路径距离而不是直线距离

完成标准：

- 每个点都能用 2 到 3 句话讲明白

---

### P2：如果时间允许可提前准备

#### 任务 8：把成员 B 后续代码模块草案补出来

目标：

- 为下周正式开发做准备

建议先规划：

- `src/search/`
- `src/recommend/`
- `src/diary/`
- `src/integration/`

完成标准：

- 至少把模块职责写清楚

#### 任务 9：先整理成员 B 的后续实现顺序

建议顺序：

1. 排序模块
2. Top-K 模块
3. 景点/学校查询
4. 模糊查询
5. 场所查询
6. 美食推荐
7. 日记查询与推荐

---

## 4. 本周交付物清单

本周结束前，成员 B 至少应提交以下内容：

- `docs/architecture/成员B架构与逻辑方案.md`
- Mermaid 架构图
- 业务功能与算法映射表
- AI 使用截图 2 到 3 张
- 一份可直接给 PPT 使用的文字整理

如果进度较好，可额外补：

- 接口依赖说明
- 成员 B 代码目录草案

---

## 5. 按天执行建议

### 2026-04-04（周六）

今天优先做：

1. 完成架构文档
2. 完成 Mermaid 架构图
3. 完成算法映射表
4. 整理 AI 使用截图

今天结束前的理想状态：

- 你的材料已经可以交给组长或负责做 PPT 的同学

### 2026-04-05（周日）

明天优先做：

1. 检查 PPT 是否正确引用你的内容
2. 检查讲解逻辑是否把成员 B 的职责讲清楚
3. 若发现表述不清，及时补一版精简文字

---

## 6. 本周不需要你主负责的内容

根据 `第五周.md`，以下内容不是成员 B 的主责任：

- 录制最终讲解视频
- 提交周报到教学云平台
- 主导 200+ 节点数据生成脚本
- 主导哈夫曼压缩和倒排索引详细实现

你需要做的是：

- 保证你的设计方案足够完整
- 保证你的部分能被顺利讲解
- 为下周编码做好结构准备

---

## 7. 我们接下来可以按什么顺序协作

建议后续按下面顺序推进：

1. 先完成 `docs/architecture/成员B架构与逻辑方案.md`
2. 再补 Mermaid 架构图
3. 再补算法映射表
4. 再整理 PPT 可用版内容
5. 最后再补代码目录草案和接口草案

---

## 8. 结论

本周对成员 B 来说，最关键的不是“功能写了多少”，而是先把“你负责什么、为什么这样设计、后续怎么实现”讲清楚、写清楚、交付清楚。

只要先把下面四件事完成，本周就算完成核心任务：

1. 架构文档
2. 架构图
3. 业务与算法映射表
4. AI 使用截图
