# 软件开发任务描述

> 文档类型：软件开发任务描述（对应课程「应完成的文档资料」第 1 项）  
> 课程设计题目：个性化旅游系统的设计与实现

---

## 一、任务背景

随着假期旅游需求的增长，用户在旅游全流程中面临信息分散、路线规划低效、离线场景体验差等痛点。现有商业地图与旅游应用主要面向宏观城市道路导航，难以覆盖大型封闭园区（景区、校园）内的微观寻路需求，也缺乏对个人兴趣的深度适配与弱网环境下的离线支持。

本课程设计任务是在上述背景下，设计并实现一套**基于自研数据结构与核心算法**的个性化旅游系统，覆盖旅游前、旅游中、旅游后的完整使用场景，并在系统开发过程中引入智能体辅助工程化实践。

---

## 二、系统目标

开发一套真实可运行的个性化旅游系统，具体目标如下：

1. 帮助用户在旅游前根据热度、评价和个人兴趣选择旅游目的地；
2. 帮助用户在景区或校园内规划最优游览路线，支持多种交通方式与室内外导航；
3. 帮助用户在游览过程中查询附近服务设施与美食；
4. 支持用户记录、分享和检索旅游日记；
5. 所有核心功能均基于自研数据结构与算法实现，并提供多算法对比分析。

---

## 三、数据规模要求

| 指标 | 最低要求 |
|------|----------|
| 景区与校园数量 | ≥ 200 个 |
| 每处建筑物数量 | ≥ 20 个（景点、教学楼、办公楼、宿舍等） |
| 服务设施种类 | ≥ 10 种（商店、饭店、洗手间、图书馆、食堂、超市、咖啡馆等） |
| 服务设施总数 | ≥ 50 个 |
| 道路图边数 | ≥ 200 条（尽量贴近真实景区与校园） |
| 系统用户数 | ≥ 10 人 |

数据来源：高德/百度地图开放 POI 接口采集，辅以公开数据集冷启动，统一清洗为 JSON/CSV 结构化格式，由成员 C 负责维护数据字典与版本管理。

---

## 四、功能需求

### 4.1 旅游推荐

- 按热度、评价、个人兴趣推荐景点与学校；
- 支持按名称、类别、关键字查询，查询结果可按热度与评价排序；
- **核心算法**：Top-K 推荐（最小堆/快速选择，避免全量完全排序）、排序算法；
- **创新点**：支持用户自定义多维度权重（如热度 40% + 评分 30% + 距离 30%）的加权融合排序；支持错别字容错、拼音首字母、同义词等模糊查询增强。

### 4.2 旅游路线规划

- 单目标最优路线：从当前位置到指定景点或场所；
- 多目标路线：从当前位置出发，访问多个目标点后返回起点；
- 支持图形化地图展示与路径高亮；
- **路线策略**：
  - 最短距离策略；
  - 最短时间策略（考虑拥挤度，真实速度 = 拥挤度 × 理想速度）；
  - 交通工具约束策略（校区：步行/自行车；景区：步行/电瓶车；支持混合交通工具）；
  - 室内导航策略（大门→电梯→楼层→房间）；
- **核心算法**：Dijkstra 最短路径、状态压缩动态规划（多目标回路）、多层图建模；
- **创新点**：引入时间维度动态边权（4D 时空动态权值），实现智能避峰与路线动态自适应规划；室内外跨层一体化图结构，支持多交通工具无缝切换。

### 4.3 场所查询

- 选中景点或场所后，查询一定范围内的服务设施，按**真实路径距离**排序（禁止使用直线距离）；
- 支持按类别过滤；
- 支持输入类别名称查找附近设施；
- **核心算法**：查找算法、排序算法（基于图上最短路径距离）。

### 4.4 旅游日记管理

- 用户可通过文字、图片、视频记录旅游日记；
- 统一管理所有用户日记，浏览量作为热度指标；
- 支持按热度、评分、个人兴趣推荐日记；
- **核心算法**：排序算法（Top-K）。

### 4.5 旅游日记交流

- 按目的地检索相关日记，并按热度与评分排序；
- 支持日记标题精确查询（适应大规模、高动态数据）；
- 支持日记内容全文检索；
- 支持日记无损压缩存储；
- 支持基于 AIGC 的旅游动画生成（根据照片与文字描述）；
- **核心算法**：查找算法、倒排索引全文检索、哈夫曼无损压缩；
- **创新点**：在压缩后的本地数据上构建倒排索引，支持完全断网时的高效全文检索；高优离线同步机制，预先打包高价值数据保障弱网体验。

### 4.6 美食推荐

- 按热度、评价、距离排序，支持菜系过滤；
- 支持 Top-10 推荐（不经过全量完全排序）；
- 支持名称、菜系、饭店等模糊查询，查询结果可多关键字排序；
- **核心算法**：Top-K 排序（最小堆）、模糊查找算法。

---

## 五、系统架构

系统采用分层架构，核心算法模块与 UI 解耦：

```
表现层（UI / 命令行）
    ↓
业务层（成员 B：查询、排序、推荐、日记业务编排）
    ↓
算法层（成员 A：图建模与路径规划）
    ↓
数据层（成员 C：结构化数据集、全文检索、压缩）
```

**模块依赖关系**：
- 成员 C 提供结构化数据集 → 成员 A 建图寻路、成员 B 业务算法；
- 成员 A 提供真实距离与路径接口 → 成员 B 场所/美食距离排序；
- 成员 C 提供倒排索引检索结果 → 成员 B 日记全文检索展示。

---

## 六、成员分工

| 成员 | 负责方向 | 核心任务 |
|------|----------|----------|
| 成员 A | 图结构与路径规划 | 图建模（邻接表/多层图）、单源最短路（Dijkstra）、多目标回路路径、交通约束与拥挤度策略、室内分层图抽象、真实距离接口 |
| 成员 B | 查找、排序、推荐与业务交互 | 旅游推荐（Top-K）、景点/学校查询、场所查询、美食推荐、日记查询与推荐、模糊查询、统一业务输入输出 |
| 成员 C | 数据、检索、压缩、测试与文档 | 数据采集与清洗、数据字典维护、倒排索引全文检索、哈夫曼无损压缩、测试框架与联调组织、周报与验收材料 |

---

## 七、核心算法与数据结构总表

| 业务功能 | 负责成员 | 核心数据结构 | 核心算法 |
|----------|----------|--------------|----------|
| 多模式寻路 | A | 图、邻接表、多层图 | Dijkstra、状态压缩 DP、多点规划 |
| 景点/学校 Top-10 推荐 | B | 顺序表、最小堆 | Top-K（堆）、加权融合排序 |
| 景点/学校查询 | B | 哈希索引、顺序表 | 精确查找、过滤、归并排序 |
| 模糊查询 | B | Trie / 关键词索引 | 前缀匹配、编辑距离、同义词扩展 |
| 场所查询 | B + A | 分类索引 | 过滤 + 图距离 + 排序 |
| 美食 Top-10 推荐 | B + A | 索引、堆 | 过滤 + Top-K + 图距离 |
| 日记按目的地查询 | B + C | 目的地索引 | 索引 + 排序 |
| 日记标题精确查询 | B | 哈希表 | 精确查找 |
| 日记全文检索 | C | 倒排索引 | 文本搜索（TF-IDF 可扩展） |
| 日记无损压缩 | C | 哈夫曼树 | 哈夫曼编码 |

---

## 八、智能体开发计划

按课程要求，本组配置三组智能体辅助工程化：

| 智能体组 | 用途 |
|----------|------|
| 学习辅助 Agent | 解释数据结构与算法、生成学习笔记与答辩问答 |
| 协作与任务分解 Agent | 拆解周任务、生成接口草稿与进度 checklist |
| 文档与演示辅助 Agent | 润色 Markdown、生成 PPT 大纲与讲稿 |

---

## 九、开发进度安排

进度安排与课程节点对齐，同时配合成员 B 的四个里程碑（M1～M4）：

| 里程碑 | 课程节点 | 时间 | 完成标准 |
|--------|----------|------|----------|
| M1 基础可运行 | — | 第 7 周 | 景点/学校查询可用；Top-10 推荐可用；单目标最短路可演示 |
| M2 业务功能覆盖 | 第 9 周期中检查 | 第 9 周 | 场所查询、美食推荐、日记基础查询、模糊查询均可用；主链路（查景点→推荐→路线规划）联调通过 |
| M3 联调可演示 | 第 11 周答疑 | 第 12 周 | 距离排序接入成员 A 接口；全文检索与压缩接入；输出结构统一；算法对比初稿完成 |
| M4 验收可交付 | 第 15 周验收 | 第 15 周 | 全部功能可演示；算法说明、测试样例、演示链路、报告与文档完整打包 |

---

## 十、创新点总结

1. **面向微观场景的时空动态多目标导航引擎**：建立室内外多层级拓扑网络，引入时间维度动态边权，结合状态压缩 DP 与改进 Dijkstra 实现智能避峰路线规划，填补现有商业地图在封闭园区微观导航上的空白。

2. **弱网环境下的高密度离线检索与存储框架**：基于哈夫曼树对海量中文游记进行无损压缩，并在压缩后的本地数据上构建倒排索引，实现完全断网时的高效全文检索，解决偏远景区信息断层问题。

3. **可配置多目标加权推荐与模糊查询增强**：支持用户自定义热度、评分、距离等维度权重的融合排序；支持错别字容错、拼音首字母、同义词联想等模糊查询，提升真实用户输入场景下的检索命中率。

---

## 十一、交付物清单

1. 可运行的个性化旅游系统（含源码与可执行程序）
2. 结构化数据集与数据字典
3. 功能需求说明与分析文档
4. 总体方案设计说明（含智能体使用说明）
5. 数据结构说明与数据字典
6. 各模块设计说明（算法思想、复杂度分析、多算法对比）
7. 测试用例与测试结果记录
8. 用户使用说明
9. 课程设计报告（report.doc）
10. 周报（第 5～14 周，每周一份）
