# 智能校园/景区导览系统 — 项目结构 & 知识体系总结

## 一、项目定位

> **数据结构课程设计**项目，题目：**"个性化旅游系统的设计与实现"**（演化为智能校园/景区导览系统）。
> 由 **3 个 AI Agent**（Member A/B/C）协作开发。
> 三条 Agent 管线：Claude Code (A) + Codex CLI (B) + OpenClaw (C)

---

## 二、完整目录结构

```
Data-Structure-Course-Design/
│
├── AGENTS.md                    # AI Agent 团队行为准则
├── README.md                    # 项目总览
├── .gitignore
│
├── data/                        # ====== 数据层 ======
│   ├── global_sites.json        # 全局景区注册表（200+）
│   ├── users.json               # 用户样例数据（70人）
│   ├── diary_data.json          # 日记测试数据
│   ├── aigc_media_samples.json  # AIGC 媒体样例
│   ├── map_nodes.json / map_edges.json / scenic_spots.json  # 历史兼容
│   │
│   └── sites/                   # 标准分层景区数据
│       ├── PKU/                 # 物理全量数据（21个子图）
│       │   ├── outdoor.json     # 室外道路图
│       │   ├── indoor_LIB.json  # 图书馆（多楼层）
│       │   ├── indoor_TB1/2.json, DORM1.json, CHEM_A~D.json
│       │   ├── indoor_ACCEL.json, YDY31/32.json, ENGINEERING.json
│       │   ├── indoor_MATH.json, ANCIENT_LIB.json, URBAN_ENV.json
│       │   ├── indoor_QDB_SPORTS.json, WUSI_SPORTS.json
│       │   ├── indoor_CANTEEN_JIAYUAN/XUEYI/YANNAN.json
│       │   └── geo/             # OSM地理参考+室内模板
│       ├── THU/                 # 清华大学（独立数据）
│       ├── FDU/, HIT/, NJU/, ... # 20+站点
│
├── src/                         # ====== 源码 ======
│   ├── site_registry.py         # 景区注册表枢纽
│   ├── graph/                   # 图结构 (Member A)
│   │   ├── graph.py             # 邻接表+分层子图
│   │   └── loader.py            # 加载/合并/桥接
│   ├── routing/                 # 路径规划 (Member A)
│   │   └── router.py            # Dijkstra + 状态压缩DP
│   ├── search/                  # 查询检索 (Member B)
│   │   ├── search_service.py    # 业务入口
│   │   ├── fuzzy_search.py      # 模糊查询（Levenshtein+同义词）
│   │   ├── exact_search.py      # 精确查询
│   │   └── distance_adapter.py  # 距离适配器
│   ├── recommend/               # 推荐排序 (Member B)
│   │   ├── topk.py              # 小顶堆 Top-K
│   │   ├── sorter.py            # 归并排序
│   │   ├── ranking.py           # 业务排序策略
│   │   └── interest.py          # 兴趣加权推荐
│   ├── diary/                   # 日记 (Member B+C)
│   │   ├── diary_service.py
│   │   └── fulltext_service.py
│   ├── compress/                # 压缩检索 (Member C)
│   │   ├── huffman.py           # 哈夫曼编码
│   │   ├── fulltext.py          # 倒排索引全文检索
│   │   └── offline_index.py     # 离线索引包装
│   └── ui/                      # 表现层 (Member B)
│       ├── demo_server.py       # Flask Web 服务器
│       ├── demo_service.py      # UI 业务路由
│       ├── desktop_app.py       # Windows桌面入口
│       └── static/              # 前端资源
│           ├── index.html / app.js / styles.css
│           └── vendor/leaflet/  # 本地 Leaflet 地图库
│
├── tests/                       # ====== 测试层 ======
│   ├── test_graph_load.py, test_routing.py
│   ├── test_search.py, test_recommend.py
│   ├── test_diary.py, test_fulltext.py, test_compress.py
│   ├── test_integration.py, test_ui_demo.py
│   ├── test_course_requirements.py
│   └── test_m31c_*_interest.py, test_m31d_regression.py, ...
│
├── docs/                        # 项目文档
│   ├── 总体方案设计说明.md / 数据字典.md / 课程要求覆盖清单.md
│   ├── 项目代码骨架与职责划分.md / 软件开发任务.md
│   ├── 地图方案B_*.md (M7~M32各阶段)
│   └── references/map-plan-b/ (Leaflet, Mapshaper, OSM资料)
│
└── 工作进度/                    # 周报与任务清单
```

---

## 三、四层分层架构

```
 表现层 (UI/CLI)            — Flask Web + Leaflet 地图
      ↓
 业务层 (查询/推荐/日记)    — 模糊搜索、Top-K、兴趣推荐
      ↓
 算法层 (图/路径)           — Dijkstra、状态压缩 DP
      ↓
 数据层 (JSON/压缩/检索)    — 景区注册表、哈夫曼、倒排索引
```

**数据流向**：
- Member C(数据层) → Member A(建图)：loader.py 读取 data/sites/{id}/*.json
- Member C(数据层) → Member B(业务层)：search_service.py 读取节点生成记录
- Member A(距离接口) → Member B(排序)：router.query_distance() 供推荐使用
- Member C(全文检索) → Member B(展示)：fulltext.py 倒排索引

---

## 四、核心数据结构

### 4.1 图结构 (Graph)

| 概念 | 实现 |
|------|------|
| 邻接表 | `adj: dict[str, list[dict]]` |
| 节点属性表 | `nodes: dict[str, dict]` |
| 多层图（分层） | `sub_graphs: dict[str, Graph]` |
| 递归查找 | `find_node_recursive()` |

### 4.2 小顶堆 (MinHeap) → Top-K

- 自定义堆实现：`_shift_up` / `_shift_down`
- 降序 Top-K：**原值入堆**，堆顶为第 K 大最小值
- `O(n log k)`，远优于 `O(n log n)`

### 4.3 归并排序 (Merge Sort)

- 多字段动态排序规则
- **稳定排序**，相等时保留原顺序
- `O(n log n)` / `O(n)`

### 4.4 哈夫曼树 (Huffman)

- 频率统计 → 建堆 → 两两合并 → 递归生成编码表(左0右1)
- 编码 → pack_bits → 字节流（含压缩比估算）

### 4.5 倒排索引 (Inverted Index)

- CJK n-gram 分词(2~8字窗口)
- 字段加权：title×18, tags×14, destination×10, content×6
- 同义词展开：图书馆↔library↔tsg

---

## 五、核心算法

### 5.1 Dijkstra 最短路径

```python
distances = {node: inf}
pq = [(0, start)]
while pq:
    weight, node = heapq.heappop(pq)
    if weight > distances[node]: continue
    for edge in adj[node]:
        if not is_edge_allowed(edge, mode): continue
        if new_dist < distances[neighbor]:
            distances[neighbor] = new_dist
            heapq.heappush(pq, (new_dist, neighbor))
```

**扩展**：交通方式过滤 + 时间策略(speed×congestion) + 分层感知

### 5.2 多目标 DP (Held-Karp)

```python
DP[mask][last] = min cost to visit {mask} ending at {last}
```

`O(n² · 2ⁿ)`，限制 n ≤ 12

### 5.3 模糊查询评分体系

| 级别 | 名称 | 关键词 | 标签 | 描述 |
|------|------|--------|------|------|
| 精确 | 160 | 85 | 70 | 36 |
| 前缀 | 120 | 68 | 56 | 30 |
| 包含 | 90 | 52 | 42 | 24 |
| 跳字缩写 | 72 | 42 | 34 | 16 |
| 编辑距离 | 64 | 36 | 30 | 12 |

### 5.4 兴趣推荐加权

```python
SCENIC_WEIGHTS = {
    "interest_match": 0.45,  # 兴趣匹配度
    "heat": 0.25,            # 热度
    "rating": 0.20,          # 评分
    "distance_m": 0.10,      # 距离
}
```

---

## 六、模板克隆机制（200+景区）

**核心**：180+ 景区只复用 PKU 一份物理拓扑数据。

```json
{"id": "BNU", "template_site_id": "PKU",
 "display_overrides": {
   "node_names": {"gate_north": "北师大西门"},
   "text_replacements": {"北京大学": "北京师范大学"}
 }}
```

流程：选择 BNU → `resolve_site_data_id()` 返回 PKU → 加载 PKU 图数据 → 应用 display_overrides 改名 → 展示。

---

## 七、成员职责矩阵

| 模块 | Member A (Claude Code) | Member B (Codex CLI) | Member C (OpenClaw) |
|------|----------------------|---------------------|-------------------|
| 图结构 | `graph/` ✅ | — | — |
| 路径规划 | `routing/` ✅ | — | — |
| 查询 | 提供距离接口 | `search/` ✅ | — |
| 推荐 | — | `recommend/` ✅ | — |
| 日记 | — | `diary/` (业务) | `fulltext.py` (索引) |
| 压缩 | — | — | `huffman.py` ✅ |
| UI | — | `ui/` ✅ | — |
| 数据 | — | — | `data/` ✅ |
| 测试 | — | — | `tests/` ✅ |

---

## 八、启动方式

```bash
# Web 主入口
py -m src.ui.demo_server

# 默认地址
http://127.0.0.1:8765

# Windows 桌面入口
py -B -m src.ui.desktop_app

# 运行测试
python -m pytest
```
