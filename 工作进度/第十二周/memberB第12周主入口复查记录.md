# memberB 第12周主入口复查记录

> 角色：Member B（查询、排序、推荐、业务交互与 Web 主入口负责人）
> 复查时间：2026-05-22
> 复查目标：对 Web 主入口进行一次完整浏览器链路复查，确认核心功能入口、查询 / 推荐 / 路径 / 日记 / AIGC 链路、站点切换重置和 UI 收口是否稳定可用。

---

## 1. 复查范围

本次复查覆盖以下内容：

1. 首页与工作区切换是否正常。
2. 顶部功能入口是否清晰可用。
3. 综合查询、场所查询、美食推荐是否能够正常出结果。
4. 单目标路径、多目标路径是否可从 Web 页面进入并展示。
5. 日记中心是否可以继续承接全文检索和路线跳转。
6. AIGC 轻量预览是否能返回可见结果。
7. 站点切换后，当前结果、路线和地图状态是否能正确重置。
8. 自动化测试与浏览器链路是否一致。

---

## 2. 复查方式

本次复查采用“自动化回归 + 浏览器实际操作”的方式完成：

1. 先执行核心脚本回归，确认主入口没有基础回退。
2. 再在本地临时 demo 服务上用浏览器自动化逐步点击页面入口。
3. 按普通用户路径检查功能是否能从页面直接进入、返回结果并继续联动。

复查时重点执行了以下验证：

```powershell
node --check src\ui\static\app.js
python -B tests/test_ui_demo.py
python -B tests/test_integration.py
python -B tests/test_course_requirements.py
```

---

## 3. 复查结论

本次主入口复查结论为：**通过**。

当前 Web 主入口已满足第 12 周阶段性收口要求，主要表现为：

1. 首页和工作区切换正常。
2. 顶部功能入口可直接进入各主功能。
3. 查询、推荐、路径、日记、AIGC 链路均可从 Web 页面完成。
4. 单目标与多目标路径都能返回可读结果。
5. 站点切换后，结果和路线会重置。
6. UI 自动化和核心测试保持通过。

---

## 4. 关键链路结果

### 4.1 首页与工作区

1. 首页标题正常显示。
2. 点击工作区后，当前功能会切到默认业务页。
3. 顶部功能导航可继续切换到不同业务模块。

### 4.2 综合查询

1. 输入“图书馆”后可返回结果。
2. 结果卡片可继续进入路线规划。
3. 路径结果可在地图和摘要区同步显示。

### 4.3 场所查询与美食推荐

1. 输入“洗手间”可返回场所结果。
2. 输入“食堂”可返回餐饮推荐结果。
3. 结果展示可读，且符合当前页面的统一结果卡片样式。

### 4.4 路径规划

1. 单目标路径可正常规划。
2. 页面可展示路径摘要与路径步骤。
3. 浏览器链路中，单目标结果可见且可继续联动地图高亮。

本次复查中看到的典型单目标结果为：

- 路径：`北京大学西门 -> 图书馆`
- 距离：`341.4 m`
- 时间：`4 分 33 秒`
- 步骤数：`13 步`
- 路径点数：`14 个`

### 4.5 日记中心

1. 日记中心可正常切换。
2. 全文检索可返回结果。
3. 日记结果可继续进入路线规划。
4. 日记侧的管理入口和联动按钮均可用。

### 4.6 AIGC 轻量预览

1. AIGC 页面可正常进入。
2. 输入预览提示后可生成模板化分镜结果。
3. 页面能明确展示“轻量预览”口径，不会误导为真实模型生成。

### 4.7 站点切换

1. 站点切换可触发新的 bootstrap 和地图加载流程。
2. 切换后，当前结果数会清零。
3. 路线摘要会回到“暂无路径”。
4. 路线元信息会回到未规划状态。
5. 当前功能标签保持在工作区逻辑内，不会出现页面错乱。

---

## 5. 测试结果摘要

本次复查对应的核心测试均已通过：

1. `tests/test_ui_demo.py`
2. `tests/test_integration.py`
3. `tests/test_course_requirements.py`
4. `node --check src\ui\static\app.js`

这说明本次 UI 收口没有破坏主入口、路径联动和课程规模核验。

---

## 6. 问题与说明

本次复查发现的唯一需要说明的现象是：

1. 在切换到其他站点时，页面提示过一次“底图瓦片加载异常”。
2. 但本地 GeoJSON 道路、POI 和路线仍可继续显示。
3. 这不影响当前主入口演示和验收链路，属于底图层提示，不是主功能阻塞。

当前未发现新的 P0 / P1 级阻塞问题。

---

## 7. 后续建议

1. 继续保持当前主入口收口后的结构，不再扩大新的页面层级。
2. 若后续要处理底图瓦片异常，再单独排查底图源和失败提示文案。
3. 继续同步维护 `tests/test_ui_demo.py`，避免页面结构变化后测试断言失配。
4. 第 12 周后半程优先做材料收口和演示稳定，不再扩张功能面。

---

## 8. 材料冻结前检查

本次复查材料已与当前代码状态对齐，结论为：通过。

1. 主入口、查询、推荐、路径、日记和 AIGC 链路均能从 Web 页面直接进入。
2. `tests/test_ui_demo.py`、`tests/test_integration.py`、`tests/test_course_requirements.py` 与当前页面收口一致。
3. 当前仓库内未检出 `项目交接说明.md`，因此本轮没有需要删除的交接说明文件。
4. 第十二周材料已进入冻结状态，不再为本轮复查继续扩大功能说明。
