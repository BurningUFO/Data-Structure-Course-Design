# 第十二周工作周报（5月18日 - 5月24日）

> **项目名称**：智能校园/景区导览系统
> **汇报人**：项目大总管 (Member A)
> **版本说明**：截至 2026-05-22 的联调更新版，已吸收 Member B / Member C 当前周内提交材料

本周团队核心目标为 M3“全量功能接入周”。与第十一周“课程硬指标补齐 + 正式产品骨架锁定”的阶段任务相比，第十二周的关键不再是单点补缺，而是把查询、推荐、路径、多目标路径、全文检索、压缩、日记管理和 AIGC 轻量演示这些能力统一收入口径、统一验证方式，并确认它们能稳定地从 Web 主入口直接进入和展示。

截至 2026-05-21，当前仓库已经完成第十二周第一轮主链路验证：`tests/test_routing.py`、`tests/test_integration.py`、`tests/test_course_requirements.py` 和 `tests/test_ui_demo.py` 均通过，说明当前系统已具备继续向第十三周“正式产品冻结周”推进的基础。当前阶段未发现新的路径或联调阻塞，主要剩余工作已逐步转向体验打磨、材料收口和冻结前风险控制。

---

## 1. 团队整体阶段状态

1. **Web 主入口已具备全量功能接入基础**：根据当前 `tests/test_ui_demo.py` 的通过结果，Leaflet 地图、室内导航、多目标路径、附近设施查询、日记中心和 AIGC 轻量演示入口均可从浏览器端直接进入。
2. **路径主链路与业务联调当前稳定**：根据 `tests/test_routing.py` 与 `tests/test_integration.py` 的通过结果，单目标路径、多目标路径、跨层路径、交通方式过滤、日记目的地跳转路径和全文检索后接路径规划均未出现回退。
3. **课程硬指标核验已进入稳定可执行状态**：根据 `tests/test_course_requirements.py` 的当前输出，项目已具备 `70` 个用户样例、`70` 个日记作者、`208` 条扩展推荐 / 查询对象，以及更大规模的当前 PKU 路网快照。
4. **第 13 周冻结前的主要风险已从“能不能跑”转向“怎么稳定交付”**：当前更需要关注帮助文案、空状态、错误提示、环境验证入口、轻量演示口径和周报 / 答辩材料统一，而不是再扩大核心功能范围。

---

## 2. 本周阶段性成果

### 【Member A：图结构与路径规划 / 协作统筹】

截至当前，Member A 已完成以下阶段工作：

1. 发布 `memberA第12周统筹清单.md`，明确第 12 周硬项、分工、联调节奏、验收口径和冻结前禁止事项。
2. 复核 `query_routing(...)`、`query_distance(...)`、`query_multi_target(...)` 的字段稳定性，确认当前 Web 主入口继续依赖 `visit_order`、`leg_results`、`segments`、`path_steps`、`layer_sequence` 等关键展示字段。
3. 执行本周第一轮路径与集成验证，确认当前路由主线、跨层路径、混合交通方式路径和多目标路径均可稳定返回。
4. 将第 11 周 `200` 边阶段的路径性能结论继续作为第 12 周答辩口径使用：
   - 单目标查询平均耗时维持在毫秒以下；
   - 多目标回路平均耗时约 `1 ~ 2 ms`；
   - 可支撑当前 Web 演示和课程答辩。
5. 补齐第 12 周缺失的 Member A 文档，包含工作内容陈述、第一轮串测问题清单、风险清单和本周周报阶段版。

### 【Member B：业务交互与 Web 主入口】

从当前仓库状态和 UI 测试结果可确认：

1. 当前 Web 主入口已经能稳定承接 Leaflet 地图、多目标路径、日记中心、AIGC 轻量演示和室内导航相关能力。
2. 地图区已朝“正式产品主入口”收口，当前更偏向减少误导验收的调试入口，强化主链路展示和地图快捷操作。
3. 当前 UI 自动化回归通过，说明第十二周阶段并未发生“页面能打开但主链路不可用”的回退。
4. 本周已补齐 Member B 的正式工作内容陈述与主入口复查记录，可作为第 13 周冻结前的 UI 书面证据。

### 【Member C：数据、检索、压缩、测试与文档】

从当前课程核验结果可确认：

1. 课程规模核验脚本当前可稳定执行，且输出已明显高于第 11 周的阶段性规模。
2. 当前系统已具备较大规模的用户、作者、扩展对象和节点 / 边快照，为正式产品冻结提供了较完整的数据底座。
3. 课程要求核验、UI demo、路径联调和集成主线当前未出现新的测试阻塞。
4. Member C 已补齐第 12 周课程核验与联调记录、个人工作内容陈述，并同步把课程覆盖清单与数据字典更新到当前快照口径。

---

## 3. 当前验证结果

优先验证命令：

```powershell
python -m pytest
```

结果：当前环境失败，报错 `No module named pytest`。这属于本地 Python 环境缺少 `pytest` 模块，不是当前代码回归。

因此本周当前阶段采用以下替代验证集合：

```text
python -B tests/test_routing.py
python -B tests/test_integration.py
python -B tests/test_course_requirements.py
python -B tests/test_ui_demo.py
```

当前验证结论：

1. `tests/test_routing.py`：`83` 项通过，路径字段、错误提示、跨层、多目标和交通方式过滤正常。
2. `tests/test_integration.py`：`19` 项通过，查询 / 推荐 / 路径 / 日记 / 全文检索主链路联调正常。
3. `tests/test_course_requirements.py`：全部通过，当前输出摘要为：
   - `users=70`
   - `diary_authors=70`
   - `extension_objects=208`
   - `pku_nodes=1565`
   - `pku_edges=3550`
   - `facility_like_nodes=1397`
4. `tests/test_ui_demo.py`：全部通过，Web 主入口、Leaflet 地图、室内导航、日记中心、AIGC 入口和多目标路径当前均可验收。

---

## 4. 当前限制与风险

1. **`pytest` 统一入口尚未恢复**：当前仍需依赖分测试脚本执行回归。
2. **日记与 AIGC 仍为轻量演示口径**：日记当前不写回正式数据文件，AIGC 当前仍为模板化可见预览。
3. **扩展对象不等于全量可导航对象**：当前规模核验已经达标，但 `208` 条扩展对象中只有部分承担深度导航映射角色。
4. **正式产品体验尚未完全冻结**：当前核心功能可用，但第 13 周仍需继续完成帮助文案、错误提示、响应式细节和演示模式收口。

---

## 5. 下半周计划

1. 继续维持路径接口冻结规则：如需新增字段，只允许追加，不允许改名或改语义。
2. 吸收 Member B / Member C 的第 12 周正式提交内容，在周末前完成 `第12周周报.md` 最终版。
3. 继续跟踪冻结前风险，重点盯住 Web 主入口体验、轻量演示口径、课程核验脚本与验证环境问题。
4. 为第 13 周正式产品冻结准备统一答辩口径，重点覆盖：
   - 路径性能结论
   - 多目标路径展示字段
   - 课程规模核验结果
   - 轻量日记 / AIGC 演示说明
