# memberC 第12周课程核验与联调记录

> 核验日期：2026-05-22  
> 角色：Member C（数据、检索、压缩、测试与文档负责人）  
> 目标：把第 12 周课程硬指标核验、Web 主入口联调证据和正式产品冻结前的数据口径统一到当前仓库快照。

---

## 1. 本轮固定核验范围

本轮核验不再停留在第 11 周“最低达标”口径，而是直接对第 12 周当前快照做固定回归，重点覆盖：

1. 课程硬指标脚本是否仍可稳定执行。
2. 用户样例、日记作者、扩展对象、AIGC 样例和必要文档是否齐备。
3. 查询 / 推荐 / 日记 / 全文检索 / 压缩 / 路线规划联调主链路是否回退。
4. Web 主入口、日记中心、AIGC 轻量预览、多目标路径和站点切换重置能力是否仍可验收。

---

## 2. 实际执行命令

```powershell
python -B tests/test_course_requirements.py
python -B tests/test_integration.py
python -B tests/test_ui_demo.py
node --check src/ui/static/app.js
```

补充环境检查结论：

```powershell
python -m pytest
```

当前环境仍提示 `No module named pytest`，因此本周继续以分脚本回归作为正式核验入口。

---

## 3. 当前核验结果

### 3.1 课程硬指标核验

`tests/test_course_requirements.py` 通过，当前输出摘要为：

- `users=70`
- `diary_authors=70`
- `extension_objects=208`
- `mapped_extension_objects=5`
- `pku_nodes=1565`
- `pku_edges=3550`
- `white_road_nodes=868`
- `poi_access_nodes=111`
- `facility_like_nodes=1397`

结论：

1. 第 11 周建立的最低门槛仍稳定满足。
2. 第 12 周运行时快照已经显著高于 `>=10` 用户、`>=50` 设施、`>=200` 对象、`>=200` 边的最低课程口径。
3. 当前书面证据链已包含第 11 周核验记录、第 12 周核验与联调记录、第 12 周个人工作内容陈述和课程覆盖清单 / 数据字典。

### 3.2 集成联调

`tests/test_integration.py` 通过，当前关键链路包括：

1. 查询 `图书馆` 后可进入路径规划。
2. 日记目的地查询可返回结果并继续进入路线规划。
3. 全文检索命中 `图书馆 自习` 后，可通过 `destination_node_id=library` 继续联动路径规划。
4. 哈夫曼压缩 / 解压 round-trip 一致。

当前脚本输出结论为：`19` 项通过，`0` 失败。

### 3.3 UI 主入口联调

`tests/test_ui_demo.py` 已通过，作为第 12 周 Web 主入口回归入口继续保留，主要覆盖：

1. 首页和工作区入口；
2. Leaflet 地图与室内导航入口；
3. 单目标 / 多目标路径展示；
4. 日记中心、全文检索和目的地跳转；
5. AIGC 轻量预览入口；
6. 站点切换后的状态重置。

结论：

1. 当前 Web 主入口、Leaflet 地图、室内导航、日记中心、AIGC 入口和多目标路径仍可验收。
2. 本轮未发现“页面结构已收口但自动化断言仍停留在旧版 UI”这一类回退。

### 3.4 前端脚本静态检查

`node --check src/ui/static/app.js` 继续作为成员 B / C 联调时的最低前端语法自检命令，当前未发现语法错误。

---

## 4. 当前口径统一说明

1. **课程最低门槛**：仍按 `>=10` 用户、`>=50` 设施、`>=200` 对象、`>=200` 边执行。
2. **第 12 周当前快照**：统一采用 `users=70`、`diary_authors=70`、`extension_objects=208`、`pku_nodes=1565`、`pku_edges=3550`。
3. **轻量实现口径**：日记管理当前为 `memory_only` 演示，AIGC 当前为 `template_preview` / `placeholder_ready` 轻量可见预览。
4. **扩展对象口径**：`208` 条扩展对象用于推荐 / 查询规模达标；其中仅 `5` 条保留稳定路由映射样例，不承诺全量对象均可直接深度导航。

---

## 5. 当前未见阻塞与剩余风险

本轮核验未发现新的 P0 / P1 阻塞，但仍需保持以下风险提醒：

1. `pytest` 统一入口尚未恢复。
2. 日记与 AIGC 仍为轻量演示口径，答辩与帮助说明必须保持一致。
3. 课程规模已显著扩大，后续材料不能再沿用第 11 周的 58 节点 / 200 边旧快照作为当前系统现状。
