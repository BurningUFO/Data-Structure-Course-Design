# 三模块联调问题跟踪表（第八周）

> 由 Member C 牵头整理，持续更新。
> 记录三模块联调过程中发现的接口兼容性问题。

---

## 问题记录

| 编号 | 问题描述 | 涉及模块 | 发现日期 | 优先级 | 状态 | 影响范围 | 建议方案 | 建议解决时间 |
|------|---------|---------|---------|--------|------|---------|---------|------------|
| C-001 | `data/成员Cdata/scenic_spots.json` 景点缺少 `node_id` 或 `map_node_id` 映射字段，B 无法为景点计算真实距离 | C → B | 04-24 | **高** | ⏳ 待处理 | 推荐系统距离排序功能不可用 | Member C 为景点补充 `node_id` 字段指向图节点 | 04-25 |
| C-002 | 新分层格式（`outdoor.json` 中 nodes+edges 合并）与旧 `GraphLoader.load_from_json()` 分离文件格式不兼容 | C → A | 04-24 | **高** | ⏳ 待处理 | A 的图加载无法使用新数据 | Member A 升级 loader 支持递归读取 `data/sites/` | 04-25 |
| C-003 | `NodeWithEdges.json` 使用节点内嵌邻接表格式，与标准 loader 期望的分离 `nodes` + `edges` 结构不一致 | C → A | 04-24 | 中 | ✅ 已废弃 | 该文件仅作参考 | 以标准格式为准，不修复 | — |
| C-004 | Member A `query_distance` 接口暂未包含 `site_id` 参数，无法区分多景区 | A → B | 04-24 | 中 | ⏳ 待讨论 | 多景区场景的距离查询 | A 确认是否需要分景区查询 | 04-26 |
| C-005 | Member B `search_service.py` 默认读取 `data/scenic_spots.json`，未指向分层路径 `data/sites/` | B ↔ C | 04-24 | 中 | ⏳ 待处理 | B 的数据加载路径 | 方案一：C 在标准目录补充兼容文件；方案二：B 更新加载路径 | 04-26 |
| C-006 | Member A `Router` 当前不支持跨层路径搜索（室外→室内） | A → B | — | 中 | ⏳ 待确认 | 分层路径规划功能 | A 后续开发 | 待定 |
| C-007 | 景点数据 `scenic_spots.json` 中的 `destination` 字段（日记用）与图节点 ID 未建立映射 | C → B | — | 低 | ⏳ 待规划 | 日记推荐场景 | 后续补充映射表 | 待定 |

---

## 优先级说明

| 等级 | 定义 | 处理时限 |
|------|------|---------|
| **高** | 阻塞主链路：不解决则第八周主链路无法跑通 | 24h 内 |
| 中 | 影响扩展场景：次要功能受阻，不阻塞核心流程 | 本周内 |
| 低 | 后续迭代优化 | 后续周次 |

---

## 解决进展跟踪

| 日期 | 处理内容 |
|------|---------|
| 04-24 | 初始化跟踪表，记录 C-001 ~ C-007 |
| | Member C 完成数据目录规范化和数据字典 v2.0 更新 |
| | 发现 C-001（缺 node_id）和 C-002（格式不兼容）为高优先级阻塞项 |

---

*本表由 Member C 维护，各成员请及时更新状态。*
