# 第八周工作周报（4月20日 - 4月26日）

> **项目名称**：智能校园/景区导览系统
> **汇报人**：项目大总管 (Member A)

本周团队核心目标为“系统联调与链路贯通”。在第七周各模块独立开发完成的基础上，本周已完成三方代码、真实数据、接口协议和主业务链路的首次集中联调。当前系统已跑通“输入关键字 -> 景点查询过滤 -> 距离字段补充 -> Top-K 推荐排序 -> 统一 Response -> CLI 展示”的主链路，并完成对应测试验证。

以下是本周各成员的工作总结、联调结论及后续计划：

---

## 1. 团队整体进展概览

*   **Member A (算法与图结构)**：✅ 完成交通工具过滤、多目标路径基础版、`site_id` 可选参数方案、`shortest_time` 秒级单位确认，并新增标准分层数据原生 loader。
*   **Member B (业务与推荐)**：✅ 完成查询推荐服务层拆分，正式接入 A 侧距离接口，支持基于 `distance_m` 的 Top-K 推荐排序，并输出统一 Response 与 CLI 演示。
*   **Member C (数据与集成)**：✅ 完成标准分层数据目录、`docs/数据字典.md` v2.0、缺失字段补充及《三模块联调问题跟踪表》，并锁定旧数据历史兼容口径。

---

## 2. 本周核心联调成果

1. **主业务链路贯通**：当前已可从标准分层数据中查询校园节点，并结合真实距离完成推荐排序与统一响应输出。
2. **标准数据源确定**：新开发默认使用 `data/global_sites.json` 与 `data/sites/PKU/*.json`，旧 `data/成员Cdata/scenic_spots.json` 仅保留历史兼容用途。
3. **接口协议收口**：
    - `site_id` 确定为路径接口末尾的可选参数。
    - `query_distance(..., strategy="shortest_time")` 返回单位确定为秒。
    - `query_routing(...)` 显式返回 `total_distance_m` 与 `estimated_time_s`，同时保留旧字段兼容。
4. **联调问题关闭**：本周记录的 C-001 至 C-008 均已处理；高/中优先级阻塞项全部关闭。
5. **测试验证通过**：已验证路由、图加载、搜索、推荐四类测试脚本，均可在当前主线版本下运行通过。

---

## 3. 成员详细工作汇报

### 【Member A：图结构与路径规划】

**本周主要成果：**

1. **交通工具过滤逻辑落地**：在 `src/routing/router.py` 中支持 `allowed_transports`、`transport_modes`、`transport_mode`、`blocked_transports` 与 `vehicle_access` 字段，能够根据步行、车辆等偏好过滤不可通行边。
2. **多目标路径基础版实现**：新增 `query_multi_target(...)`，采用点对点最短路径预计算与状态压缩 DP，支持多个目标点访问顺序规划和分段路径拼接。
3. **标准分层数据 loader 完成**：在 `src/graph/loader.py` 中新增 `GraphLoader.load_site_graph(site_id="PKU", ...)`，可直接读取 `data/sites/{site_id}/outdoor.json` 与 `indoor_*.json`，并自动补充室外门节点与室内入口节点之间的 `gate_link`。
4. **接口语义统一**：将 `site_id` 作为可选参数接入 `query_routing(...)`、`query_distance(...)`、`query_multi_target(...)`，并明确 `shortest_time` 返回秒数。
5. **真实数据验证完成**：已使用 `PKU` 标准分层数据验证单目标路径、跨层路径和多目标路径逻辑。

### 【Member B：查询、排序与推荐业务】

**本周主要成果：**

1. **业务服务层拆分**：新增 `src/search/search_service.py`，将查询、过滤、距离补充、推荐排序和统一响应组织为 `search_and_recommend(...)` 主入口。
2. **距离接口正式接入**：新增 `src/search/distance_adapter.py`，封装 Member A 的 `GraphLoader` 与 `Router.query_distance(...)`，使业务层无需直接依赖 A 模块内部细节。
3. **距离排序能力补齐**：新增 `src/recommend/ranking.py`，支持 `distance_m` 升序推荐，并将缺失、不可达或异常距离统一排在可计算距离之后。
4. **统一 Response 增强**：更新 `src/search/response.py`，成功与失败响应均支持 `metadata`，可展示排序策略、距离单位、距离状态统计和结果字段说明。
5. **CLI 演示入口更新**：更新 `src/search/cli_demo.py`，支持输入关键字、类别、起点节点、排序字段和返回数量，默认基于标准分层真实数据展示完整链路。

### 【Member C：数据测试与系统集成】

**本周主要成果：**

1. **数据目录规范化**：新增并维护 `data/global_sites.json`、`data/sites/PKU/outdoor.json`、`data/sites/PKU/indoor_LIB.json`，形成当前标准分层数据结构。
2. **数据字典升级**：将 `docs/数据字典.md` 升级为 v2.0，补充全局景区、节点字段、边字段、跨模块依赖关系、门节点规则和历史兼容说明。
3. **字段补充适配联调**：为边补充 `vehicle_access` 字段，支持 A 侧交通工具过滤；为节点补充 `tags`、`category`、`description` 等字段，支持 B 侧查询推荐。
4. **旧数据兼容口径锁定**：为 `data/成员Cdata/scenic_spots.json` 补充 `map_node_id` 字段，校园条目映射到标准图节点，全国景区统一置为 `null` 并由 B 侧返回 `missing_node_id`。
5. **联调问题跟踪完成**：维护《三模块联调问题跟踪表》，记录并关闭 C-001 至 C-008，明确 `destination` 字段不参与当前路径/距离计算。

---

## 4. 验证结果

本周收口时已完成以下只读核验与测试：

```text
python -B tests/test_routing.py
python -B tests/test_graph_load.py
python -B tests/test_search.py
python -B tests/test_recommend.py
```

验证结论：

*   路由模块测试通过，覆盖交通工具过滤、`site_id` 兼容、时间单位、多目标路径与跨层路径。
*   图加载测试通过，标准 `PKU` 分层图可加载，节点总数符合预期。
*   搜索模块测试通过，覆盖标准数据查询、距离补充、统一 Response、异常距离状态和旧数据兼容。
*   推荐模块测试通过，覆盖热度、评分、距离排序及距离同分处理。

---

## 5. 当前遗留风险与处理口径

1. **非阻塞数据一致性项**：`data/global_sites.json` 当前声明了 `indoor_DORM1` 子图，但 `data/sites/PKU/` 目录下实际只有 `outdoor.json` 和 `indoor_LIB.json`。当前主链路未依赖 `indoor_DORM1`，不影响第八周联调闭环；第九周需由 Member C 统一注册表与实际文件。
2. **旧景点数据仅保留历史兼容**：`data/成员Cdata/scenic_spots.json` 不再作为标准数据源。全国景区的 `map_node_id=null` 属于已确认口径，业务层应返回 `missing_node_id`，不得抛出异常。
3. **目的地推荐链路暂不纳入本周闭环**：旧数据中的 `destination` 字段当前不参与路径或距离计算。如后续扩展日记/目的地推荐，需要先补充明确的图节点映射。

---

## 6. 下周工作计划（第九周）

1. **数据一致性收口**：由 Member C 修正 `global_sites.json` 与 `data/sites/PKU/` 实际文件之间的子图声明一致性。
2. **扩展真实场景数据**：在标准分层结构下补充更多建筑、室内图或场所节点，减少对旧兼容数据的依赖。
3. **业务链路复用扩展**：Member B 继续将 `search_and_recommend(...)` 复用于美食、场所、洗手间等查询场景。
4. **路径算法增强**：Member A 继续优化多目标路径规划策略，并结合交通工具偏好完善不同出行方式的路径结果展示。
5. **集成验收准备**：三方继续补充测试覆盖和演示脚本，为课程设计验收材料与最终展示做准备。
