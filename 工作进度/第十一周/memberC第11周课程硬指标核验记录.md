# memberC 第11周课程硬指标核验记录

> 核验日期：2026-05-10  
> 角色：Member C（数据、检索、压缩、测试与文档负责人）  
> 目标：先补齐第十一周可落地的课程硬指标底座，并记录第十二周必须达标的数据缺口。

---

## 1. 已补齐项

| 项目 | 当前结果 | 相关文件 |
|------|----------|----------|
| 用户样例 | `data/users.json` 共 10 个用户 | `data/users.json` |
| 日记作者数 | `data/diary_data.json` 共 12 篇日记，独立 `author_id` 为 10 个 | `data/diary_data.json` |
| 媒体占位 | 4 篇日记已补 `images` 占位路径 | `data/diary_data.json` |
| AIGC 样例 | 3 条“图片占位 + 文字描述 -> 预览占位”样例 | `data/aigc_media_samples.json` |
| 字段说明 | 已补用户样例、媒体占位和 AIGC 样例字段口径 | `docs/数据字典.md` |

---

## 2. 当前数据规模快照

| 指标 | 当前值 | 结论 |
|------|--------|------|
| PKU 标准分层图节点数 | 34 | 可支撑核心站点深度导航，但服务设施总量仍不足 |
| PKU 标准分层图边数 | 66 | 距离 `>=200` 道路边仍有缺口 |
| PKU 节点分类数 | 约 14 类 | 基本满足 `>=10` 服务设施种类口径，需脚本固化 |
| 旧扩展推荐 / 查询对象池 | 48 条 | 距离 `>=200` 对象仍有缺口 |
| 旧扩展对象中可路由对象 | 5 条 | 可继续作为“推荐对象 -> 路径规划”的桥接样例 |

---

## 3. 第11周核验口径

本周新增 `tests/test_course_requirements.py`，采用以下口径：

1. 对第十一周承诺完成的底座项做强断言：10+ 用户、10+ 日记作者、必要文档、AIGC / 媒体样例。
2. 对第十二周才要求完全达标的规模项先输出统计，不让本周测试因尚未扩容的数据失败。
3. 在工作陈述和周报中明确记录未达标差距，避免把 `200+` 对象、`50+` 服务设施、`200+` 边数继续后移到第 14-15 周。

---

## 4. 第12周补齐路径

- 将 `data/成员Cdata/scenic_spots.json` 或后续标准扩展对象池补到 `>=200` 条。
- 扩展 PKU 核心导航图，使道路边数向 `>=200` 收口，并交由 Member A 做路径性能验证。
- 补足服务设施总数到 `>=50`，保持类别、标签、`map_node_id` 与搜索推荐逻辑兼容。
- 将 `tests/test_course_requirements.py` 中的统计项升级为正式达标断言。
