# memberC 第11周课程硬指标核验记录

> 核验日期：2026-05-10  
> 角色：Member C（数据、检索、压缩、测试与文档负责人）  
> 目标：补齐第十一周课程硬指标底座，并将数据规模项升级为可脚本核验的达标状态。

---

## 1. 已补齐项

| 项目 | 当前结果 | 相关文件 |
|------|----------|----------|
| 用户样例 | `data/users.json` 共 10 个用户 | `data/users.json` |
| 日记作者数 | `data/diary_data.json` 共 12 篇日记，独立 `author_id` 为 10 个 | `data/diary_data.json` |
| 媒体占位 | 4 篇日记已补 `images` 占位路径 | `data/diary_data.json` |
| AIGC 样例 | 3 条“图片占位 + 文字描述 -> 预览占位”样例 | `data/aigc_media_samples.json` |
| 字段说明 | 已补用户样例、媒体占位和 AIGC 样例字段口径 | `docs/数据字典.md` |
| 扩展推荐 / 查询对象 | 208 条 | `data/成员Cdata/scenic_spots.json` |
| PKU 服务设施总数 | 服务设施口径节点 53 个 | `data/sites/PKU/*.json` |
| PKU 道路图边数 | 分层图合计 200 条边 | `data/sites/PKU/*.json` |

---

## 2. 当前数据规模快照

| 指标 | 当前值 | 结论 |
|------|--------|------|
| PKU 标准分层图节点数 | 58 | 已超过 20+ 建筑 / 景点 / 楼宇节点口径 |
| PKU 标准分层图边数 | 200 | 已达到 `>=200` 道路边口径 |
| PKU 节点分类数 | 14 类 | 已满足 `>=10` 服务设施种类口径 |
| 旧扩展推荐 / 查询对象池 | 208 条 | 已达到 `>=200` 推荐 / 查询对象口径 |
| 旧扩展对象中可路由对象 | 5 条 | 可继续作为“推荐对象 -> 路径规划”的桥接样例 |
| 服务设施口径节点 | 53 个 | 已达到 `>=50` 服务设施总数口径 |

---

## 3. 第11周核验口径

本周新增并升级 `tests/test_course_requirements.py`，采用以下口径：

1. 对第十一周承诺完成的底座项做强断言：10+ 用户、10+ 日记作者、必要文档、AIGC / 媒体样例。
2. 对数据规模项做强断言：200+ 扩展对象、50+ 服务设施、200+ 分层图边。
3. 对数据质量做基础断言：用户 / 节点 / 扩展对象 ID 唯一，边端点均存在，扩展对象保留至少 5 个可路由映射样例。

---

## 4. 第12周补齐路径

- 成员 A 基于 200 条边的 PKU 图补充路径性能验证和典型演示样例。
- 成员 B 接入日记 CRUD、AIGC 可见入口和媒体占位展示。
- 成员 C 继续优化扩展对象真实性、分类覆盖和最终验收材料。
