# memberB第11周基线测试记录

> 任务阶段：任务 2，跑当前基线测试，记录修改前状态
> 测试日期：2026-05-11
> 测试目的：在成员 B 开始第十一周功能改造前，确认远端基线代码的可运行状态，后续用于区分原有问题和新增回归。

---

## 1. 测试基线

当前代码基线：

```text
6d5e93c (HEAD -> main, origin/main, origin/HEAD) data: complete week 11 course scale targets
```

当前说明：

- 本地 `main` 已对齐 `origin/main`。
- 测试前尚未修改业务代码。
- 当前成员 B 仅新增第十一周任务文档和接口边界确认记录。

---

## 2. 测试环境

Python 版本：

```text
Python 3.13.2
```

命令兼容性说明：

- 按周任务文档中的 `py -3 -B ...` 运行时，当前环境返回 `exit=112`，提示 `No installed Python found!`。
- 该问题属于本机 Python Launcher `py` 的解释器解析问题，不是项目代码测试失败。
- 改用 `python -B ...` 后，全部基线测试通过。

后续本机建议命令：

```text
python -B tests/test_xxx.py
```

如需完全遵循周任务文档中的 `py -3 -B`，需要后续单独修复本机 Python Launcher 配置。

---

## 3. 基线测试结果

| 测试脚本 | 结果 | 说明 |
|----------|------|------|
| `tests/test_graph_load.py` | 通过 | 旧地图与标准 PKU 分层图加载正常 |
| `tests/test_routing.py` | 通过 | 路由模块单元测试通过 |
| `tests/test_search.py` | 通过 | 查询、模糊查询、距离适配、场所查询均通过 |
| `tests/test_recommend.py` | 通过 | 排序、Top-K、美食推荐均通过 |
| `tests/test_diary.py` | 通过 | 日记查询、旧数据兼容、全文检索均通过 |
| `tests/test_integration.py` | 通过 | 第九至第十周端到端联调 19 项通过 |
| `tests/test_fulltext.py` | 通过 | 倒排索引与离线全文检索通过 |
| `tests/test_compress.py` | 通过 | 哈夫曼压缩 / 解压测试通过 |
| `tests/test_ui_demo.py` | 通过 | 第十周最小 Web UI 服务层测试通过 |
| `tests/test_course_requirements.py` | 通过 | 第十一周课程硬指标核验通过 |

汇总：

```text
10 个测试脚本全部通过，失败 0 项。
```

---

## 4. 关键输出摘要

### 4.1 图与路径

- 标准 PKU 分层图加载成功。
- 当前 PKU 图规模：58 节点。
- 集成测试中多目标路径可运行，示例总距离为 `420.0m`。
- 室外到室内路径可达，例如 `gate_north -> lib_reading_room_1`。
- 宿舍跨层路径可达，例如 `gate_north -> dorm1_room_101`。

### 4.2 查询与推荐

- 精确查询、类别过滤、关键词过滤和模糊查询通过。
- 场所查询支持真实路径距离排序。
- 美食推荐支持 Top-K、距离排序和菜系过滤。
- 距离适配层能正常调用成员 A 的路由距离接口。

### 4.3 日记、全文检索与压缩

- 默认日记数据可加载。
- 旧日记数据兼容读取通过。
- 日记标题查询、目的地查询、热度 / 评分排序通过。
- 日记全文检索支持单关键词和多关键词。
- 哈夫曼压缩 / 解压一致性通过。

### 4.4 Web 演示服务层

- `DemoUIService.get_bootstrap_payload()` 能返回地图、控件和统计数据。
- 综合查询结果可直接关联路径目标。
- 场所查询距离排序通过。
- 日记全文检索结果可关联路径目标。
- 单目标路径 overlay 能提示室内段信息。

### 4.5 课程硬指标

当前 `tests/test_course_requirements.py` 核验结果：

- 用户数：10。
- 日记独立作者数：10。
- AIGC 样例数：3。
- 含媒体占位日记数：4。
- 必要课程文档数：7。
- PKU 节点数：58。
- PKU 边数：200。
- PKU 类别数：14。
- 服务设施口径节点数：53。
- 扩展推荐 / 查询对象数：208。
- 已映射扩展对象数：5。

---

## 5. 修改前状态结论

当前远端基线在本机使用 `python -B` 运行时整体通过，可以作为成员 B 第十一周后续开发的稳定起点。

后续如果出现测试失败，应优先检查是否由本周新增改动引入；除非复现到本记录中的 `py -3` 命令兼容问题，否则不应把失败归因为基线代码本身。

下一步：

- 进入任务 3：正式 Web 主入口骨架。
