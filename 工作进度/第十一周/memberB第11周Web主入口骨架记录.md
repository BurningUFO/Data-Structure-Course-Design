# memberB第11周Web主入口骨架记录

> 任务阶段：任务 3，正式 Web 主入口骨架
> 完成日期：2026-05-11
> 目标：把第十周最小演示页升级为第十一周正式产品主入口骨架，先固定首页、站点、主导航和帮助入口。

---

## 1. 本次完成内容

### 1.1 服务层 bootstrap 扩展

更新 `src/ui/demo_service.py`，在 `get_bootstrap_payload()` 中新增：

- `product`：产品名称、当前阶段、负责人。
- `sites`：站点列表、当前站点标记、站点描述和子图信息。
- `navigation`：正式主导航定义。
- `help`：启动命令、浏览器地址、推荐演示链路和当前检查点。

这样前端不再只依赖写死文案，可以通过服务层拿到正式产品骨架所需信息。

### 1.2 页面结构升级

更新 `src/ui/static/index.html`，新增：

- 正式产品头部。
- 站点选择器。
- 帮助入口按钮。
- 顶层主导航。
- 功能入口概览卡片容器。
- 帮助说明面板。

保留并继续复用原有能力：

- 综合查询。
- 场所查询。
- 美食推荐。
- 日记全文检索。
- 单目标路径规划。
- 地图和结果展示区域。

### 1.3 前端交互骨架升级

更新 `src/ui/static/app.js`，完成：

- 顶层导航、侧边表单导航、功能卡片共用同一套 `data-tab` 切换逻辑。
- 站点选择器渲染。
- 产品标题、阶段、站点、导航、帮助内容渲染。
- 功能入口概览卡片渲染。
- 帮助说明渲染。
- 当前功能说明随导航切换更新。

说明：

- 当前远端数据只有 `PKU` 一个站点，因此站点选择器先展示结构。
- 真正的页面内站点切换状态重置放到任务 4 继续实现。

### 1.4 样式升级

更新 `src/ui/static/styles.css`，新增：

- 产品头部样式。
- 站点选择器和帮助按钮样式。
- 顶层主导航样式。
- 功能入口卡片样式。
- 帮助面板样式。
- 移动端响应式布局适配。

---

## 2. 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/ui/demo_service.py` | 修改 | 新增产品、站点、导航、帮助 bootstrap 数据 |
| `src/ui/static/index.html` | 修改 | 新增正式头部、站点选择器、主导航、帮助面板 |
| `src/ui/static/app.js` | 修改 | 新增导航切换、站点选择器、功能卡和帮助渲染 |
| `src/ui/static/styles.css` | 修改 | 新增正式主入口骨架样式和响应式适配 |
| `tests/test_ui_demo.py` | 修改 | 增加正式产品骨架 bootstrap 断言 |

---

## 3. 当前边界

本任务已经完成：

- 首页 / 产品头部。
- 站点选择器展示。
- 统一主导航结构。
- 帮助入口。
- 功能概览卡片。
- 保持原查询、推荐、日记检索和单目标路径链路可用。

后续任务继续完成：

- 任务 4：站点状态与统一页面反馈。
- 任务 5：多目标路径业务接入。
- 任务 6-7：日记 CRUD 与日记中心页面。
- 任务 8：AIGC 轻量演示入口。

---

## 4. 测试结果

已执行：

```text
python -B tests/test_ui_demo.py
```

结果：

```text
All UI demo service tests passed.
```

结论：

- 正式主入口骨架所需 bootstrap 字段已可用。
- 第十周已有查询、推荐、日记全文检索和单目标路径服务层能力未被破坏。
