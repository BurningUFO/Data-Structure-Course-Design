# 第13周高优问题清单

> 维护人：Member A
> 更新日期：2026-05-25
> 目标：只跟踪会影响正式产品冻结的 P0 / P1 问题；非阻塞体验项延期到第14周处理。

---

## 1. 当前冻结结论

截至 2026-05-25 开工检查，当前没有发现 P0 阻塞问题。第13周冻结重点是保持现有主链路稳定，不新增课程硬功能，不扩大扩展对象可导航承诺。

---

## 2. 高优问题台账

| 编号 | 优先级 | 状态 | 问题 | 当前处理 |
|------|--------|------|------|----------|
| W13-A-01 | P1 | 观察中 | Web 主入口页面结构和帮助口径需要冻结 | 已要求第13周只做文案和体验收口，不做大改版 |
| W13-A-02 | P1 | 已恢复 | `py -3 -m pytest -q` 全量入口可能耗时过长 | 已用 300 秒超时重跑通过：373 passed in 62.25s；仍保留最小冻结回归集合用于拆分定位 |
| W13-A-03 | P1 | 观察中 | 日记与 AIGC 为轻量演示，容易被误解为完整生产能力 | 页面和文档统一标注 `memory_only` / `template_preview` 边界 |
| W13-A-04 | P1 | 观察中 | 扩展推荐对象达到 208 个，但仅部分可深度导航 | 对外固定“PKU 深度导航 + 扩展对象池”口径，不承诺全量直连路线 |
| W13-A-05 | P2 | 观察中 | 浏览器验收可能命中 8765 旧进程 | 每次浏览器验收前先执行 `netstat -ano | findstr :8765` |

---

## 3. 路由接口冻结保护项

第13周除 P0 / P1 修复外，不删除、不重命名、不改变以下字段语义：

- `/api/route`：`start_node_id`、`target_node_id`、`start_node_name`、`target_node_name`、`path`、`path_node_names`、`path_steps`、`segments`、`layer_sequence`、`total_distance_m`、`estimated_time_s`、`strategy`、`transport_mode`、`ui`、`summary`。
- `/api/route/multi`：`visit_order`、`visit_order_names`、`target_node_ids`、`leg_results`、`segments`、`path_steps`、`total_distance_m`、`estimated_time_s`、`route_type`、`ui`、`summary`。

如确需新增字段，只允许追加，并同步检查 Web 展示和测试。

---

## 4. 固定演示输入

| 场景 | 输入 | 讲解重点 |
|------|------|----------|
| 单目标路线 | `gate_north -> library` | 最短距离、真实道路高亮、路径步骤 |
| 多目标路线 | `gate_north -> [library, canteen]` | 访问顺序、分段摘要、总距离 / 总时间 |
| 纯室内路线 | `library -> lib_reading_room_1` | 室内图、楼层信息、室内步行语义 |
| 跨层路线 | `gate_north -> lib_reading_room_1` | 室外到室内自动衔接、`gate_link` |
| 交通方式对比 | `gate_south -> sports_ground` | 步行 / 自行车 / mixed 最短时间差异 |
| 不可达 / 异常 | 不存在节点或不兼容交通方式 | 返回明确失败提示，不伪造路线 |

---

## 5. 验证口径

冻结周优先执行统一入口：

```powershell
py -3 -m pytest -q
```

如需拆分定位，再执行：

```powershell
node --check src\ui\static\app.js
py -3 -B tests\test_routing.py
py -3 -B tests\test_integration.py
py -3 -B tests\test_course_requirements.py
py -3 -B tests\test_ui_demo.py
```

浏览器验收前执行：

```powershell
netstat -ano | findstr :8765
```
