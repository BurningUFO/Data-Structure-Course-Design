- **完成本周核心任务**：
  1. **产出第一版数据标准**：在 `data/` 目录下创建 `scenic_spots.json` 和 `map_nodes.json` 的**最小样例集**。
  2. **编写数据字典**：在 `docs/` 目录下创建 `数据字典.md`，明确定义 `id` (字符串), `heat` (浮点数), `rating` (浮点数) 等字段标准。
- **提交了**：
  - 样例数据文件存放在 `data/` 目录。
  - 数据字典存放在 `docs/` 目录。


