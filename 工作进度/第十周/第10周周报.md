# 第十周工作周报（5月4日 - 5月10日）

> **项目名称**：智能校园/景区导览系统
> **汇报人**：项目大总管 (Member A)

本周团队核心目标为“M3 联调能力预热与离线检索落地”。在第九周完成 M2 业务覆盖和期中展示收口后，本周已补齐课程设计中的全文检索、无损压缩/解压、统一输出深化、最小可演示 Web UI 与算法对比初稿，并继续保证“查询/推荐 -> 真实距离 -> 路径规划”主链路未回退。当前系统已经具备“日记全文检索 -> 命中结果 -> 目的地节点 -> 路径规划”“日记正文压缩 -> 解压校验”以及“本地 Web 页面统一演示查询、推荐、日记和路径”的最小可演示闭环。

以下是本周各成员的工作总结、验收结论及后续计划：

---

## 1. 团队整体进展概览

*   **Member A (算法与图结构)**：✅ 已完成路径展示字段增强、标准图策略对比样例整理、交通方式差异验证和“日记结果 -> 路径规划”链路可路由性检查。
*   **Member B (业务与推荐/UI)**：✅ 已完成全文检索业务接入、统一响应结构深化、模糊查询同义词/首字母增强、`--week10` CLI 演示入口扩展和最小可演示 Web UI 初版。
*   **Member C (数据与检索/压缩)**：✅ 已完成倒排索引全文检索基础版、哈夫曼压缩/解压基础版、离线索引预留入口、对应测试与数据字典同步。

---

## 2. 本周核心收口成果

1. **全文检索能力落地**：标准 `data/diary_data.json` 已可支持单关键词、多关键词和别名词的日记正文检索，返回 `diary_id`、`title`、`matched_terms`、`score`、`destination_node_id` 等字段。
2. **压缩模块最小闭环完成**：当前已能对样例日记正文执行哈夫曼压缩、解压和一致性校验，并输出原文大小、位流大小与估算压缩包大小。
3. **业务层统一输出继续收口**：场所查询、美食推荐、日记基础查询、日记全文检索已统一到 `success`、`message`、`query_type`、`filters`、`metadata`、`results` 这一套响应口径。
4. **M2 主链路未回退**：场所查询、美食推荐、真实距离排序和路径规划接口继续保持可运行，且第十周新增能力没有破坏第九周演示入口与集成测试。
5. **最小可演示 Web UI 初版完成**：本地 Web 入口已覆盖站点概览、景点查询、场所查询、美食推荐、日记全文检索、路线规划、地图高亮和室内路径步骤说明。
6. **M3 预热材料开始沉淀**：A 侧已形成第十周算法对比说明，C 侧已形成检索与压缩验证记录，B 侧已形成第十周业务接入与 UI 展示说明。

---

## 3. 成员详细工作汇报

### 【Member A：图结构与路径规划】

**本周主要成果：**

1. **路径展示字段增强**：在 `src/routing/router.py` 中以追加字段方式补充 `start_node_name`、`target_node_name`、`path_node_names`、`path_steps`、`layer_sequence`、`route_overview` 等结果信息，方便 B/C 侧直接展示。
2. **分段结果可解释性增强**：为 `segments[*]` 增加 `segment_index`、`edge_names`、`edge_types`、`node_count`、`edge_count` 等字段，降低业务层二次拼装成本。
3. **策略对比样例整理完成**：在标准 `PKU` 图上确认 `gate_north -> lib_reception` 的距离/时间策略差异样例，并补充交通方式过滤测试。
4. **日记到路径规划链路验证**：已验证标准日记中的 `destination_node_id` 可直接被路径规划接口消费，例如 `canteen`、`library` 等节点均能稳定返回路径。
5. **算法材料沉淀**：提交 `工作进度/第十周/memberA第10周算法对比说明.md`，覆盖最短距离 vs 最短时间、交通方式差异和多目标状态压缩 DP 的适用范围。

### 【Member B：查询、排序、推荐业务与 UI 展示】

**本周主要成果：**

1. **全文检索业务入口接入**：在 `src/diary/diary_service.py` 中新增 `search_diaries_fulltext(...)`，并通过 `src/diary/fulltext_service.py` 适配成员 C 的全文检索实现与异常回退逻辑。
2. **统一响应字段深化**：更新 `src/search/response.py`，在保留 `data` 兼容的同时正式补充 `results` 字段，统一第十周对外口径。
3. **模糊查询增强**：更新 `src/search/fuzzy_search.py`，补充类别/业务关键词同义词归一化和拼音缩写最小支持，例如“厕所 -> 洗手间”“tsg -> 图书馆”。
4. **第十周 CLI 演示入口扩展**：扩展 `src/search/cli_demo.py`，新增 `--week10` 演示模式，覆盖全文检索、路径提示、场所/美食回归和压缩摘要展示。
5. **最小可演示 Web UI 建设**：新增 `src/ui/demo_service.py`、`src/ui/demo_server.py` 和 `src/ui/static/` 页面文件，通过 `py -m src.ui.demo_server` 启动本地 Web 页面，统一展示查询、推荐、日记检索和路径规划结果。
6. **联调断言补齐**：更新 `tests/test_search.py`、`tests/test_diary.py`、`tests/test_integration.py`，新增 `tests/test_ui_demo.py`，补充第十周业务入口、跨模块联调和 UI 服务层验证。

### 【Member C：数据、检索、压缩与测试】

**本周主要成果：**

1. **倒排索引全文检索实现**：新增 `src/compress/fulltext.py`，采用轻量 `CJK n-gram` + 多字段倒排索引，支持标题、正文、目的地和标签检索。
2. **哈夫曼压缩基础版实现**：新增 `src/compress/huffman.py`，完成字符频率统计、哈夫曼树构建、编码表生成、压缩与解压恢复。
3. **离线链路预留入口**：新增 `src/compress/offline_index.py`，为“压缩数据上的检索”提供内存级草稿包与统一调用边界。
4. **测试覆盖扩充**：新增 `tests/test_fulltext.py`、`tests/test_compress.py`，并更新 `tests/test_integration.py`，补齐全文检索、离线索引和压缩一致性验证。
5. **文档同步完成**：更新 `docs/数据字典.md`，补充日记数据字段、全文检索/压缩模块口径和新增文件说明。

---

## 4. 验证结果

本周收口时已完成以下核验与测试：

```text
py -3 -B tests/test_graph_load.py
py -3 -B tests/test_routing.py
py -3 -B tests/test_search.py
py -3 -B tests/test_recommend.py
py -3 -B tests/test_diary.py
py -3 -B tests/test_integration.py
py -3 -B tests/test_fulltext.py
py -3 -B tests/test_compress.py
py -3 -B tests/test_ui_demo.py
```

**验证结论：**

*   图加载测试通过，标准 `PKU` 分层图加载正常。
*   路由模块测试通过，覆盖展示字段、距离/时间策略差异、交通方式过滤和日记目的地节点可路由性。
*   搜索与推荐测试通过，说明场所查询、美食推荐及模糊查询增强未回退。
*   日记业务测试通过，说明标题查询、目的地查询、全文检索和空查询降级逻辑可用。
*   全文检索与压缩测试通过，说明第十周新增的倒排索引、离线索引草稿和哈夫曼 round-trip 已闭环。
*   集成测试通过，说明“全文检索 -> 路径规划”和“压缩 -> 解压校验”已经可以作为第十周统一演示链路。
*   UI 服务层测试通过，说明本地 Web 页面所需的地图初始数据、查询结果、日记检索结果和路径高亮数据可以稳定生成。

---

## 5. 当前限制与处理口径

1. **全文检索仍为基础版**：当前未实现复杂中文分词、TF-IDF/BM25、短语查询或布尔查询，排序以轻量覆盖度加权为主。
2. **压缩模块仍为内存级结构**：当前已能完成压缩/解压和体积估算，但还不是最终的落盘压缩容器格式。
3. **离线索引暂未持久化到磁盘**：`offline_index.py` 当前只提供运行时草稿包，用于验证调用边界与恢复正文逻辑。
4. **缺失 `destination_node_id` 的日记结果按展示降级处理**：业务层当前不会对这类结果强制提供路径提示，避免误导联调。
5. **Web UI 当前为最小可演示版本**：第十周优先覆盖主链路展示，第十一周起继续补产品骨架、站点切换、日记中心、演示模式和验收脚本。

---

## 6. 下周工作计划（第十一周）

1. **继续优化全文检索可解释信息**：评估是否补字段级命中信息、摘要生成和更稳定的评分策略。
2. **确定压缩模块后续接入边界**：决定第十一周是否将压缩 / 解压从命令级演示升级为业务层正式入口。
3. **评估离线索引持久化格式**：由 Member C 继续推进草稿包是否需要落盘及其序列化格式。
4. **推进 Web UI 产品化骨架**：由 Member B 继续把第十周最小可演示 UI 升级为正式本地 Web 主入口，补首页结构、导航、站点选择和统一页面状态。
5. **围绕第十二周 M3 节点做统一演示收口**：继续整理算法对比、测试样例、Web UI 操作路径和 CLI 兜底命令，确保后续联调和答辩可直接复用。
